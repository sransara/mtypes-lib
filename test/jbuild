(jbuild_version 1)

(executable
    ((name docedit)
    (modules (docedit))
    (libraries (mtypes))))
    
(executable
    ((name samples)
    (modules (samples))
    (libraries (mtypes))))
    
(executable
    ((name taskmaster)
    (modules (taskmaster))
    (libraries (mtypes unix))))